#!/bin/bash

# Add any startup directives here to fix up the path
export PATH=/opt/graalvm-community-openjdk-17.0.9+9.1/bin:/workspace/upper-division-cs/scripts:${PATH}
export PATH=/home/gitpod/.local/bin:${PATH}